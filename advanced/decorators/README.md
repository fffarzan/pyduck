### Decorator

A decorator super charges a function. It wraps another function and enchances it.

### HOF (Higher Order Function)

It's a function that either accepts a function as input or return a function as output

TODO: Work on del and delete operators in Python and JS
